CompileFlags:
  # ESP-IDF writes the current compilation database into the build tree.
  # Keeping this pointed at the project root made clangd consume a stale copy
  # with paths from the old source layout.
  CompilationDatabase: build
